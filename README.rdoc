== README

Fake Bank - User can register for a new account, login once registered and view and create transactions

Run `rails server`  in terminal to open app

In browser, navigate to localhost:3000 to view app

Navigate to `http://localhost:3000/sessions/new` to login or register
